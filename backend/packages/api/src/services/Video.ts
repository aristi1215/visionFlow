import { supabase } from "../integrations/supabase.js";


//Probably this service won't be used in the future
class Video {
    static async createVideo(video: any) {};
    static async deleteVideo(id: string) {};
    static async getVideoById(id: string) {};
}